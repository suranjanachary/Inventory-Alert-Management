spring:
  datasource:
    url: jdbc:mysql://localhost:3306/inventory_alert
    username: inventory
    password: inventory
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE
